# ABX Orchestration Page Optimization Plan

Overhaul the current [ABXOrchestration.tsx](file:///Users/user/Documents/Command%20Center%20Cockpit/Canopi_Antigravity/canopi---intel-excels/src/pages/ABXOrchestration.tsx) into a high-fidelity, operational "Journey Coordination Command Center".

## User Review Required

> [!IMPORTANT]
> This is a massive update. I will be adding substantial new code and data structures within [ABXOrchestration.tsx](file:///Users/user/Documents/Command%20Center%20Cockpit/Canopi_Antigravity/canopi---intel-excels/src/pages/ABXOrchestration.tsx). I will preserve existing data values (142, 84%, etc.) but within much richer UI components.

## Proposed Changes

### [ABXOrchestration](file:///Users/user/Documents/Command%20Center%20Cockpit/Canopi_Antigravity/canopi---intel-excels/src/pages/ABXOrchestration.tsx)

#### [MODIFY] [ABXOrchestration.tsx](file:///Users/user/Documents/Command%20Center%20Cockpit/Canopi_Antigravity/canopi---intel-excels/src/pages/ABXOrchestration.tsx)

**1. Data & State Initialization**
- Define robust mock data for:
    - `buyingCommittee`: Matrix of accounts vs roles with status (identified, engaged, champion, risk).
    - `channelPressure`: Matrix of accounts/stages vs channels with intensity/efficacy.
    - `activityTimeline`: List of events with icons, owners, and account linkage.
    - `decisions`: List of blockers and pending approvals.
    - `pipeline`: Compact stats on ops, wins, losses, projects, and whitespace.

**2. Layout Layers**
- **Layer 1: Context & Filters**: Functional Hero + Enriched KPIs + Top Navigation Filters.
- **Layer 2: Real-time Journey**: Augmented Progression block with velocity analytics.
- **Layer 3: Orchestration Heatmaps**: The two primary heatmaps (Committee & Pressure).
- **Layer 4: Operational Pulse**: Coordinated Timeline + Decisions/Blockers.
- **Layer 5: Campaign & Plays**: Active campaign cards + Operational plays.
- **Layer 6: Pipeline & Expansion**: The new "Commercial Memory" layer (Projects, Whitespace, Events).
- **Layer 7: Analysis & Fila**: ABX-specific charts + Operational Fila (Table).

**3. Component Enhancements**
- Implement `MatrixHeatmap` for both Committee and Pressure views.
- Implement `VerticalTimeline` for activities.
- Implement `ExpansionMap` (Whitespace) using a grid layout.
- Use `framer-motion` for transitions between "Operational" and "Executive" views if possible.

## Verification Plan

### Manual Verification
1.  **Visual Identity**: Ensure the page looks distinct from ABM Strategy (more flow-oriented).
2.  **Data Integrity**: Check that the original KPIs are still present and accurate.
3.  **Heatmap Interactivity**: Hover over cells in the new heatmaps to see the "Rich Tooltip" with contact details.
4.  **Operational Fila**: Verify sorting and filtering in the new operational table.
5.  **Responsiveness**: Check the layout on different screen widths (xl vs md).
6.  **Drill-down**: Verify that clicking a row/account opens a detailed state (or a placeholder modal/drawer).
